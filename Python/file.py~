import os
import os.path

PATH='/home/sdshakya/Desktop/Python/file.py'

if os.path.isfile(PATH) and os.access(PATH, os.R_OK):
    print "File exists and is readable"
else:
    print "Either file is missing or is not readable"
